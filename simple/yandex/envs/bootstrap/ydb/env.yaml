# Module envs
ydb_database_serverless:
  # name: ydb
  name_prefix: ydb-bootstrap-terraform
tables:
  - path: state-lock-table
    primary_key: ["LockID"]
    columns:
      - name: LockID
        type: String